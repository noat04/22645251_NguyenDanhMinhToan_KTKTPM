module.exports = {
    secret: "your-secret-key",
    refreshSecret: "your-refresh-secret-key",
    jwtExpiration: 3600,           // 1 giờ (tính bằng giây)
    jwtRefreshExpiration: 86400,    // 24 giờ
};